# Deploy checklist — my-agents1234 (dev)

Generated by **AgentForge**. Review every file before apply.

## Prerequisites

- AWS account **948451199137**, region **us-east-1**.
- CI auth: **GitHub OIDC → IAM role**.
- Terraform **>= 1.10** (if using Terraform; S3 `use_lockfile`) or CDK CLI (if using CDK).
- GitHub OIDC trust on IAM role `my-agents1234-dev-deploy` (see `oidc-trust-policy.json`).
- S3 state bucket **my-agents1234-dev-tfstate** exists **before** first `terraform init` (native S3 lockfile; Terraform >= 1.10).

## IAM deploy role

- Role ARN: `arn:aws:iam::948451199137:role/my-agents1234-dev-deploy`
- Attach policies your pipelines need (Terraform/CDK/CloudFormation execution, and DevOps Agent AWSCC resources if applicable).

## Order of operations

1. Create OIDC provider + IAM role trust (GitHub: `token.actions.githubusercontent.com`).
2. Create the S3 state bucket (versioning recommended).
3. `terraform init` → `terraform plan` in `infrastructure/terraform/dev`.
4. If **AWS DevOps Agent** resources are included, ensure `awscc` provider credentials and service quotas.
5. Merge pipeline workflow files (role ARN includes account `948451199137` from your AgentForge project).
6. Push to GitHub — open a PR to run **terraform plan** (see `GITHUB-SETUP.md`).

## Troubleshooting (including with AI)

If `terraform plan` or `apply` fails:

1. Run `terraform validate` in `infrastructure/terraform/dev` first.
2. Copy the **full error block** (resource name, file, line, message).
3. In Cursor (or another AI assistant), paste the error plus the matching `.tf` file — ask to align with current AWS/awscc provider schemas.
4. Regenerate from AgentForge after updating the app, or patch the repo and re-run plan.

Set `create_github_oidc = false` in a `terraform.tfvars` if the GitHub OIDC provider already exists in the account.

## Docs

- [AWS DevOps Agent User Guide](https://docs.aws.amazon.com/devopsagent/latest/userguide/)
- [GitHub OIDC with AWS](https://docs.github.com/en/actions/deployment/security-hardening-your-deployments/configuring-openid-connect-in-amazon-web-services)
