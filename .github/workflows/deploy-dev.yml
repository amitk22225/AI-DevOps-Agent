name: Deploy my-agents1234 (dev)

on:
  push:
    branches: [main]
    paths: ['infrastructure/terraform/dev/**', '.github/workflows/**']
  pull_request:
    branches: [main]
    paths: ['infrastructure/terraform/dev/**']
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: deploy-my-agents1234-dev
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: dev
    defaults:
      run:
        working-directory: infrastructure/terraform/dev
    steps:
      - uses: actions/checkout@v4
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::948451199137:role/my-agents1234-dev-deploy
          aws-region: us-east-1
      - uses: hashicorp/setup-terraform@v3
      - run: terraform init
      - run: terraform fmt -check -recursive
        continue-on-error: true
      - run: terraform validate
      - name: Terraform Plan
        run: terraform plan -no-color -out=tfplan
      - name: Upload plan
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-dev
          path: tfplan
          retention-days: 5
      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
